{
  "name": "blog",
  "private": true,
  "scripts": {
    "lint": "prettier --check .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "prettier-plugin-toml": "^2.0.6"
  }
}
